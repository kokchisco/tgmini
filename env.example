########################################
# Minimal env required for production  #
########################################

# Telegram Bot (required)
BOT_TOKEN=your_telegram_bot_token_here
BOT_USERNAME=your_bot_username_here

# Web App URL (your deployed HTTPS URL ending with /webapp)
WEBAPP_URL=https://your-domain.com/webapp

# Database (use Postgres in production)
# Example: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql://username:password@host:5432/tgtask

# Admin (protects admin APIs)
ADMIN_USER_ID=your_telegram_user_id_here

# Optional
# Public image/file_id sent with /start (photo caption + Open Mini App button)
WELCOME_IMAGE_URL=

# Runtime config (points/withdrawal/currency/rates/app name/support username)
# are managed in the Admin panel and stored in DB. Do NOT set those here.

# Node options
NODE_ENV=production
WEBAPP_PORT=3001
SEED_SAMPLES=false
